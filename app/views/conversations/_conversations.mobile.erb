<%= render conversations %>
<div class="pagination">
  <%= _("No hay ninguna conversación.") if conversations.count == 0 %>
  <%= pager_total conversations %>
</div>
<div class="converses">
  <%= paginate conversations %>
</div>