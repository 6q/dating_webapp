<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%=  yield(:title) + " | " if content_for?(:title) %>Cellove.com</title>
    <!--[if IE]>
    <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <%= javascript_include_tag "application" %>
    
    <script src="/javascripts/jquery.flexslider-min.js"></script>
    <script src="/javascripts/functions.js"></script>

    <link href='http://fonts.googleapis.com/css?family=PT+Sans' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <% if @page == 'home' %>
      <style>
        body {
          background:url('/stylesheets/images/bg/main-bg<%= Random.rand(5) + 1 %>.jpg') left -25px no-repeat;
        }
      </style>
      <% else %>
      <style>
        header[role="banner"] {
          background:url('/stylesheets/images/bg/main-bg<%= Random.rand(5) + 1 %>.jpg') top left no-repeat;
        }
      </style>
    <% end %>
    <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Cellove" %>">
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">
    <header role="banner" class="clearfix">
      <div class="container">
        <%= render 'layouts/header-login' %>
        <h1><%= link_to "Cellove", root_path, :class => 'brand' %></h1>
        <h2>Ponle alas al amor</h2>
        <%#= render '/shared/public/main-nav' %>
        <%= render 'layouts/navigation' %>
      </div>
    </header>
    <div class="container">
      <section role="main">
        <% if @sidebar %>
          <div class="row">
          <div class="span3">
            <% if @isblog %>
              <%#= render '/shared/public/blog-sidebar' %>
            <% else %>
              <%#= render '/shared/public/flat-sidebar' %>
            <% end %>
          </div>
          <div class="span9">
            <% if @isblog %>
              <div id="box-blog">
            <% else %>
              <div class="box">
            <% end %>
              <div class="box-content">
                <%= render 'layouts/messages' %>
                <%= yield %>
              </div>
            </div>
          </div>
        <% else %>
          <%= render 'layouts/messages' %>
          <%= yield %>
        <% end %>
      </section>
    </div>
    <%#= render '/shared/public/in-love' %>
    <%#= render '/shared/public/footer' %>  
    </div>
  </body>
</html>
  </body>
</html>
