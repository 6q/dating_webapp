<% title = "Contacta" %>
<% content_for(:title) {title} %>
<article>

  <header class="bg clearfix">
    <h1>&nbsp;<br><%= title %><br>&nbsp;</h1>
    <%= image_tag 'teaser-contact.jpg' %>
  </header>

  <div class="article-body">
    <div class="row">
      <div class="span6">
        <h3>Somos un equipo joven y con vocación de dar lo mejor de nosotr@s para facilitar y mejorar las relaciones personales de amor y amistad.</h3>
      </div>

      <div class="span6">
        <%= simple_form_for @contact_form, url: contact_form_path, :html => {:class => 'form-horizontal' } do |f| %>
          <fieldset id="contact" class="clearfix form-horizontal">
            <p>Si tienes cualquier duda o pregunta sobre Cellove, su funcionamiento o, simplemente quieres ponerte en contacto con nosotr@s, hazlo a través del siguiente formulario:</p>           
            <%= f.input :name %>
            <%= f.input :surname %>
            <%= f.input :email %>
            <%= f.input :message, as: :text, input_html: {rows: '2'} %>
            <div class="actions">
              <%= f.button :submit, class: 'btn btn-info btn-large', value: 'ENVIAR' %>
            </div>
          </fieldset>
        <% end %>
      </div>
    </div>
  </div>
</article>
