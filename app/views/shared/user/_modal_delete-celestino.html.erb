<!-- Modal -->
<div id="delete-celestino-<%= recommendation.id %>" class="modal hide fade" tabindex="-1" role="dialog" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
  </div>
  <div class="modal-body">
    <div class="center">
      <p>Estás a punto de eliminar a un Celestin@ de tu lista. Si lo haces, lo que ha dicho sobre ti desaparecerá de tu perfil. ¿Te lo has pensado bien?</p>
    </div>
  </div>
  <div class="modal-footer center">
    <%= link_to 'No, creo que lo consideraré de nuevo', '#', 'data-dismiss' => 'modal', 'aria-hidden' => 'true' %>
    <%= link_to _('Si, quiero eliminarlo'), recommendation_deny_path(recommendation.id), class: 'btn btn-danger', :method => :post %>
  </div>
</div>
