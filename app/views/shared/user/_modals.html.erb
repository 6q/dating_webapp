<% #TODO: Stop using samples, maybe we will have to use ajax queries %>
<%= render partial: 'shared/user/modal_fast-reply', locals: {user: User.all.sample} %>
<%= render partial: 'shared/user/modal_activity', locals: {user: User.all.sample} %>
<%= render 'shared/user/modal_delete-confirm' %>
<%= render 'shared/user/modal_delete-celestino' %>
<%= render 'shared/user/modal_answer-proposal' %>
