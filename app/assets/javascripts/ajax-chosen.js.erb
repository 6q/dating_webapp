$("#example-input").ajaxChosen({
    type: 'GET',
    url: 'countries_path',
    dataType: 'json'
  }, function (data) {
    var results = [];

    $.each(data, function (i, val) {
      results.push({ value: val.value, text: val.text });
    });

    return results;
  }
});

