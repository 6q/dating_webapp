# Use the README file as a reference to customize settings.

UserBlock:
  fields:
    default: user_id, blocked_user_id, created_at
    form: user_id, blocked_user_id
  relationships: 
  application: Application
  search: user_id, blocked_user_id
  order_by: -created_at